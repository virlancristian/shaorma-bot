const { specialMsgVerifier } = require('messages/verifiers/special-message');

const checkAndReturnSpecialMsg = (message) => {
    const channel = message.client.channels.cache.get(message.channelId);
    const reply = specialMsgVerifier(message);

    if(reply != null) {
        try {
            channel.send(reply);
        } catch(error) {
            console.log(error);
        }
    }
};

module.exports = checkAndReturnSpecialMsg;